apiVersion: v1
kind: Secret
metadata:
  name: wordpress-test-secret
stringData:
  wordpress.username: johndoe
  wordpress.password: S3cr3tP@ssw0rd
  wordpress.email: johndoe@example.com
  mariadb-root-password: S3cureDBP@ss
  mariadb-password: Sup3rS3cureP@ss
  # WP_HOME and WP_SITEURL are automatically injected by configExtraInject (default: true)
  # using the wordpress.url value. No need to define them here.
  wordpress.configExtra: |-
    define('WP_MEMORY_LIMIT', '256M');
    define('WP_MAX_MEMORY_LIMIT', '512M');
    define('FORCE_SSL_ADMIN', false);
    define('WP_CACHE', true);
  wordpress.metrics.token: f5634d6a966856848e2f3f4a139e534b844805f7561d86642adb19060719e95d
type: Opaque
